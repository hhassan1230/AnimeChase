
<%= "You have no animes saved" if @animes.empty? %>

<% @animes.uniq.each do |anime| %>
  <img src="<%= anime.img_url %>" height="250" >
  <%= link_to anime.title, "/anime/#{anime.slug}"  %></br>
  <%= anime.description %></br></br>

<% end %>


